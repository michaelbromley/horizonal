var themes = themes || {};

themes["Slide Show"] = {
    options: {
        customCssFile: 'themes/slideshow.css',
        stagger: 'random',
        staggerDelay: 0,
        selector: 'p,img,h1,h2,h3, h4, .h, .thumbnail, em, li',
        rootElement: '#root',
        pageHideDelay: 3
    }
};